[project]
name = "privacy-falcon-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "pymongo",
  "redis",
  "rq",
  "python-dotenv"
]
