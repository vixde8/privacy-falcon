{
  "name": "privacy-falcon",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "vitest": "^4.0.15"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
